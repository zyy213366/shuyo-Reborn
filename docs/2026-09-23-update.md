# 2026-09-23 本次修改

- 课表对比：选择至少两张课表后回到首页。共同课程正常高亮、其余变暗；共同空闲显示所有人的占用时间并集。使用真实日期和各课表的节次钟点，保留课间空白。可翻周，范围覆盖参与课表与调休。
- 翻页：继续使用已有 PageController 与拖动，预布局相邻周，缓存稳定 5/7 天周页面并隔离绘制。拖动开始、跨过半页和收尾不重建已缓存课表；数据或显示设置变化时失效；不缓存缩放中间宽度。标题仍跟随占多数的周页面。测试验证复用、同步与连续短滑；未做真机帧率测量。
- 参考范围仅 MySUES 的 schedule_screen.dart PageView/Controller：其标准 PageView 自带拖动，也在 onPageChanged setState。没有复制源码；不能将 ShuYo 卡顿笼统归咎于 setState，已针对课表重复构建与相邻页首次布局整改。
- 考试：更多 → 考试周 → 导入考试安排。沿用 WebVPN，进入教务考试安排后点击查询，再点击底部导入。抓取已验证路径 `/jwglxt/kwgl/kscx_cxXsksxxIndex.html?doType=query&gnmkdm=N358105` 的实际 POST 响应，不构造未知参数。
- 考试解析来自用户提供的响应：kcmc、kssj、cdmc、cdxqmc、ksmc、kch、jxbmc、xnmc/xqmmc。示例未提供座位与考试方式，不猜测。测试夹具移除了姓名、学号等身份字段。
- 考试以独立考试数据保存，用周视图显示；相同考试重新导入更新时间地点，保留本地座位与备注；保留其他考试和普通课程。需移除取消考试时手动删除，不根据筛选查询推断删除。分页不完整时拒绝导入并提示显示全部后查询。
- 抓取脚本只接受对应学校域名及指定查询URL；新查询清空旧响应，拒绝较早请求晚到、HTTP失败或登录页响应。首次查询早于注入且表格未保留原始数据时，需要在网页再点击一次查询。

验证：课表对比/翻页相关19项测试、考试解析/重导入/异常数据、考试增删改及失败保存、WebVPN/direct抓取脚本、真实结构考试周截图。学校实时登录仍需用户账号操作，本次未冒用账号进行联网查询。

参考：https://github.com/HsxMark/MySUES/blob/main/lib/screens/schedule_screen.dart
